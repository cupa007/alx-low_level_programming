#include <stdio.h>
/**
 * main-entry point for the program.
 *
 * Return:0 if no errors, return non zero if errors
 */
int main(void)
{
	printf("with proper grammar, but te outcome is a piece of art,\n");
	return (0);
}
